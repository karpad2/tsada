# Development Dockerfile for TSADA (Vite SPA) with hot reload
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose development port
EXPOSE 5173

# Run Vite dev server
CMD ["npm", "run", "dev"]